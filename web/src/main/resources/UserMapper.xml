<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.growup.serfj.dao.UserDAO">


    <insert id="insert" parameterType="com.growup.serfj.pojo.User" flushCache="true">
    insert into t_user(NAME , AGE) VALUES( #{name},#{age})
</insert>
    <update id="update" parameterType="com.growup.serfj.pojo.User">
	UPDATE t_user SET name=#{name},age=#{age} WHERE id=#{id}
</update>
    <delete id="delete" parameterType="String">
	DELETE FROM t_user WHERE id=#{id}
</delete>
    <select id="findById" parameterType="String" resultType="com.growup.serfj.pojo.User">
	SELECT * FROM t_user WHERE id=#{id}
</select>

    <select id="queryAll" resultType="com.growup.serfj.pojo.User">
	SELECT * FROM t_user
</select>

</mapper>